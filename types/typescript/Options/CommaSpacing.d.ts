export type Options = [
    {
        /**
         * @default false
         */
        before?: boolean;
        /**
         * @default true
         */
        after?: boolean;
    }
];

export default Options;
